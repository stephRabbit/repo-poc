import { DefaultTheme } from 'styled-components'
const theme: DefaultTheme = {
  mode: 'dark',
  colors: {
    bg: 'var(--black)',
    content: 'var(--grey)',
    textColor: 'var(--white)',
    accent: 'var(--yellow)',
    accentMedium: 'var(--pink)',
    accentDark: 'var(--purple)',
    searchColor: 'var(--pink)',
  },
}

export default theme
