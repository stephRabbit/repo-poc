import { DefaultTheme } from 'styled-components'
const theme: DefaultTheme = {
  mode: 'light',
  colors: {
    bg: 'var(--white)',
    content: 'var(--light-grey)',
    textColor: 'var(--black)',
    accent: 'var(--dark-purple)',
    accentMedium: 'var(--pink)',
    accentDark: 'var(--purple)',
    searchColor: 'var(--purple)',
  },
}

export default theme
